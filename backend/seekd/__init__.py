"""seekd — the seek AI-native IM backend daemon.

This is the only backend. Clients (TUI / GUI / WEBUI) talk to it over the
WebSocket protocol defined in CONTRACT.md. No client imports this package.
"""

__version__ = "0.1.0"
